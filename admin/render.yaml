services:
  - type: web
    name: parkmark-admin
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        value: https://parkmark.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
